apiVersion: v1
kind: Pod
metadata:
  name: pod-hostpath
  namespace: audit-zone
spec:
  containers:
    - name: nginx
      image: nginx
      volumeMounts:
        - name: host-data
          mountPath: /host
  volumes:
    - name: host-data
      hostPath:
        path: /data